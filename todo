
    // Dash in air. Seperate ability.
    // Stop flying
    // Twitter
    // Ledge

    // Ground dash. Cancel/enter sprint if pressing right.
    // Air acceleration
    // Stop multiple air attacks

    // Multiple hitboxes per ability. Transfer hit-targets for combo logic

    // Seperate fx from dash. Spawn fx.

    // Tidy up stats

    // Fix run to walk phasing


    // Add cooldown to ability slots

    // Prop and projectile animation

    // Batch render tiles

    // Remove old input mech

    // Player manager (good for multipler too, and add player (from network))
    // Another color samurai
